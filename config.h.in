/* config.h.in.  Generated by Meson; do not edit.  */

/* Define to 1 if __fsetlocking() is available. */
#mesondefine HAVE___FSETLOCKING

/* Define to 1 if feof_unlocked() is available. */
#mesondefine HAVE_FEOF_UNLOCKED

/* Define to 1 if fgets_unlocked() is available. */
#mesondefine HAVE_FGETS_UNLOCKED

/* Define to 1 if fts_open() is available. */
#mesondefine HAVE_FTS_OPEN

/* Define to 1 if getgrnam_r() is available. */
#mesondefine HAVE_GETGRNAM_R

/* Define to 1 if building with regex support. */
#mesondefine HAVE_REGEX

/* Define to 1 if stpcpy() is available. */
#mesondefine HAVE_STPCPY

/* Define to 1 if strverscmp() is available. */
#mesondefine HAVE_STRVERSCMP

/* Define to 1 if targeting PCRE2. */
#mesondefine USE_PCRE2

#ifndef HAVE___FSETLOCKING

#include <stdio.h>

static inline int __fsetlocking(FILE *stream, int type)
{
	return 1;
}

#endif

#ifndef HAVE_FEOF_UNLOCKED

#include <stdio.h>

static inline int feof_unlocked(FILE *stream)
{
	return feof(stream);
}

#endif

#ifndef HAVE_FGETS_UNLOCKED

#include <stdio.h>

static inline char *fgets_unlocked(char *s, int n, FILE *stream)
{
	return fgets(s, n, stream);
}

#endif

#ifndef HAVE_STPCPY

#include <string.h>

static inline char *stpcpy(char *dest, const char *src)
{
	strcpy(dest, src);
	return dest + strlen(dest);
}

#endif
